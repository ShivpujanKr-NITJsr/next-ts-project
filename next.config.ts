import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['www.google.com'], // Add any other external domains as needed
  },
};

export default nextConfig;
